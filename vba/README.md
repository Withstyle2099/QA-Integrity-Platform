# Simple User Form Example

This directory contains a minimal VBA example that creates a user form at runtime. To try it out:

1. Open the VBA editor (Alt + F11 in Excel).
2. Import `SimpleUserForm.bas` into a new module.
3. Run `ShowSimpleForm` to display the form.

The form is generated entirely through code and includes a label and a button to close the form.
